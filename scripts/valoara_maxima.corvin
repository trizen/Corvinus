#!/usr/bin/ruby

var(a, b);
citeste(Num, &a, &b);

func max(a, b) {
    daca (a > b) {
        return a;
    }
    altfel {
        return b;
    }
}

spune ("Max: ", max(a, b));
spune ("Sum: ", a + b);
