#!/usr/bin/ruby

var str = "acesta este un string";
spune str.caracterul(0);
spune str.caracterul(1);

spune str.in_litere_mari;
spune str.cu_prima_litera_mare;

spune str.extrage(0, 6);
spune str.contine("este");
